# @nifrajs/ui-registry

The registry is the source of truth for the docs catalogue, the source installer, and agent discovery. It contains no hosted state, credentials, telemetry, or private product metadata.
